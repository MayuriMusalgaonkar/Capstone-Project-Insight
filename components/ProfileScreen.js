import React, { useEffect, useState } from "react";
import {
  StyleSheet,
  Text,
  SafeAreaView,
  StatusBar,
  FlatList,
  View,
  ActivityIndicator
} from "react-native";

const Profile = ()=>{
  return(
    <SafeAreaView>
      <Text>
        Profile Page
      </Text>
    </SafeAreaView>
  )
}

export default Profile